let limite = 10;

for(let i=1; i<=limite;i++){

    for(let j=1; j<=limite;j++){
        console.log(i)
    };
    
};

// Ele entra no loop, repeti 10x e sai do loop, após isso entra no loop repete 10x e segue assim...