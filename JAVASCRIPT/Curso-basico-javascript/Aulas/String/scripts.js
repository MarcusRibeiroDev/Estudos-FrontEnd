// Tipo primitivo

let mensagem = 'O Brasil será campeão da copa do mundo';

// Tipo objeto

let outraMensagem = new String('Bom dia');

/* Alguns métodos

mensagem.length();
outraMensagem[4];
mensagem.includes(); Mostra se tem aquilo na string
mensagem.startsWith(); Mostra se a primera coisa é ou não e se for retorna um boolean
mensagem.endsWith(); Mostra se a ultima coisa é ou não e se for retorna um boolean
mensagem.replace(); Recebe 2 parâmetros, o que será substituido e quem irá substituir;
mensagem.trim(); Tira espaços excedentes;
mensagem.split(); Dividi pelo parâmetro e cria um array

*/


